# Procfile for running all services
# Use with goreman: https://github.com/mattn/goreman
# Install: go install github.com/mattn/goreman@latest
# Run: goreman start

collector: cd collector-service && make run
worker: cd worker-service && make run
query: cd query-service && make run
