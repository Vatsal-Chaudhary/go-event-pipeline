apiVersion: v1
kind: Secret
metadata:
  name: event-pipeline-secrets
  namespace: event-pipeline
type: Opaque
stringData:
  # -- Replace with your RDS connection string from: terraform output -raw rds_endpoint
  # Format: postgres://<user>:<password>@<rds-endpoint>:5432/<db>?sslmode=require
  DB_URL: "REPLACE_ME"
  # -- Optional: only needed if NOT using IRSA / instance-profile for S3 access
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""
